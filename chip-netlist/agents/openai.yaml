interface:
  display_name: "Chip Netlist Reverse Engineering"
  short_description: "Extract AI-ready connectivity from .epro2 projects and infer chip settings"
  default_prompt: "Use $chip-netlist to extract AI-ready connectivity from this .epro2 project, then infer chip configuration with the PDF data sheet if provided."
policy:
  allow_implicit_invocation: true
